# Below the download locations for third-party components are defined
# partimage-ng
# -> we will download a static (and small) partimage-ng version
#
OPENQRM_SOURCE_VERSION="r80"
OPENQRM_SOURCE_BINARY_RESULT=""
if [ -f /etc/debian_version ]; then
    OPENQRM_SOURCE_DOWNLOAD="http://www.openqrm-ng.net/downloads/plugins/local-storage/openqrm-partimage-ng-$OPENQRM_SOURCE_VERSION.ubuntu.tgz"
elif [ -f /etc/redhat-release ]; then
    :
elif [ -f /etc/SuSE-release ]; then
    :
fi