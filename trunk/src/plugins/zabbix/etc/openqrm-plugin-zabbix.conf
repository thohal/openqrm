# this is the openQRM-plugin-zabbix info file

OPENQRM_PLUGIN_VERSION="4.5.1.0"

# Those items setting up the dependencies for the package
if [ -f /etc/debian_version ]; then
	# OPENQRM_PLUGIN_DEPENDENCIES="zabbix-server-mysql, zabbix-frontend-php, zabbix-agent, openqrm-server"
    OPENQRM_PLUGIN_DEPENDENCIES="openqrm-server"
	OPENQRM_PLUGIN_BUILD_REQUIREMENTS=""
elif [ -f /etc/redhat-release ]; then
	OPENQRM_PLUGIN_DEPENDENCIES="zabbix, zabbix-agent, openqrm-server"
	OPENQRM_PLUGIN_BUILD_REQUIREMENTS=""
elif [ -f /etc/SuSE-release ]; then
	OPENQRM_PLUGIN_DEPENDENCIES="openqrm-server"
	OPENQRM_PLUGIN_BUILD_REQUIREMENTS=""
fi

OPENQRM_PLUGIN_DESCRIPTION="The zabbix-plugin automatically monitors the systems and services managed by the openQRM-server."
OPENQRM_PLUGIN_TYPE="monitoring"