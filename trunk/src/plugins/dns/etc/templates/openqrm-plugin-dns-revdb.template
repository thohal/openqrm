;
; BIND reverse data file 
;
$TTL	604800
@	IN	SOA	ns.@@OPENQRM_SERVER_DOMAIN@@. root.@@OPENQRM_SERVER_DOMAIN@@. (
			      1		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;

	IN	NS	ns.@@OPENQRM_SERVER_DOMAIN@@.

@@OPENQRM_SERVER_IP_LAST_BYTE@@			IN	PTR	ns
@@OPENQRM_SERVER_IP_LAST_BYTE@@			IN	PTR	openqrm
